zerops:
  - setup: api
    build:
      base: nodejs@20
      buildCommands:
        - pnpm i
        - npm run start
      deployFiles:
        - /
    run:
      base: nodejs@20
      ports:
        - port: 3000
          httpSupport: true
      start: npm start